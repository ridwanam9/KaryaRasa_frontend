const HelpSection = () => {
    return (
        <div className="help-section">
            <p className="help-text">Have a question? Well, we've got some answers.</p>
            <a href="#">
                <button className="help-button">Go to Help Center</button>
            </a>
        </div>
    );
};

export default HelpSection;